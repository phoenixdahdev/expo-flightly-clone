/**
 * Route stub for the search tab trigger. The trigger is `disabled` in the
 * layout, so this screen is never focused — its tabPress listener opens the
 * Add Flight sheet instead.
 */
export default function SearchTabPlaceholder() {
  return null;
}
